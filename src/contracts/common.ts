/**
 * Shared primitive aliases. Frozen at contracts v1.0.0.
 * Owner: noktos-agent-backend. Vendored read-only into the frontend.
 */
export type UUID = string;
/** ISO-8601 timestamp in UTC, e.g. "2026-09-11T09:30:00.000Z". */
export type ISO8601 = string;
